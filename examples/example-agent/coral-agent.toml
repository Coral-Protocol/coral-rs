[agent]
name = "example-agent"
version = "0.0.1"

[options]
OPENAI_API_KEY = { type = "string", description = "The OpenAI key to use for this agent" }

[runtimes.executable]
command = "debug/example-agent.exe"